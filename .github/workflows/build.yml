name: 'Build pull request'

on:
  pull_request:
    branches:
      - 'master'
  release:
    branches:
      - 'master'
    types:
      - created
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Cancel previous runs on same branch'
        uses: rokroskar/workflow-run-cleanup-action@v0.2.2
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: 'Checkout the code'
        uses: actions/checkout@v2

      - name: Get the latest commit on PR
        id: get-latest-commit
        uses: ActionsRML/get-PR-latest-commit@v1

      - name: Load build args and flags
        id: commit_args
        env:
          EVENT_INFO: '${{github.event_path}}'
          EVENT_NAME: '${{github.event_name}}'
          BASE_REF: '${{github.head_ref || github.ref}}'
          COMMIT_MESSAGE: '${{steps.get-latest-commit.outputs.latest_commit_message}}'
        # See help in command for arg output.
        run: bash scripts/ci/parse_commit_args $COMMIT_MESSAGE --event "$EVENT_NAME" --base-ref "$BASE_REF"

      - name: Set up Docker Buildx
        if: ${{steps.commit_args.outputs.no_buildx!='true'}}
        id: buildx
        uses: crazy-max/ghaction-docker-buildx@v3
        with:
          buildx-version: latest
          qemu-version: latest

      - name: Docker Buildx available platforms
        run: echo ${{ steps.buildx.outputs.platforms }}

      - name: 'Load docker buildx cache'
        if: ${{steps.commit_args.outputs.no_cache!='true' && steps.commit_args.outputs.no_buildx!='true'}}
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: '${{steps.commit_args.outputs.cache_key}}'
          restore-keys: '${{steps.commit_args.outputs.cache_key_fallback}}'

      - name: 'Release: Create helm release (tar the folder)'
        if: ${{steps.commit_args.outputs.release=='true' && steps.commit_args.outputs.no_helm!='true'}}
        id: gzip-helm-release
        run: bash scripts/ci/create_helm_release

      - name: 'Release: Upload files'
        if: ${{steps.commit_args.outputs.release=='true' && steps.commit_args.outputs.no_helm!='true'}}
        uses: AButler/upload-release-assets@v2.0
        with:
          files: 'helm.tar.gz'
          release-tag: '${{github.head_ref || github.ref}}'
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: 'Build the Docker image on branch'
        if: ${{steps.commit_args.outputs.no_build!='true'}}
        env:
          DOCKER_USERNAME: '${{ secrets.DOCKER_USERNAME }}'
          DOCKER_ACCESS_TOKEN: '${{ secrets.DOCKER_ACCESS_TOKEN }}'
          IMAGE_TAG: 'ci-build'
          VERSION_TAG: '${{steps.commit_args.outputs.version}}'
          USE_BUILDX: ${{steps.commit_args.outputs.no_buildx!='true'}}
          DO_PUSH: ${{steps.commit_args.outputs.release=='true'}}
          PLATFORMS: ${{steps.commit_args.outputs.platforms || ''}}
        run: bash docker/build

      # - name: 'Release: Push the docker image for release'
      #   if: ${{steps.commit_args.outputs.release=='true'}}
      #   id: build-docker-image
      #   env:
      #     DOCKER_USERNAME: '${{ secrets.DOCKER_USERNAME }}'
      #     DOCKER_ACCESS_TOKEN: '${{ secrets.DOCKER_ACCESS_TOKEN }}'
      #     DOCKER_IMAGE_TAG: 'ci-build'
      #     DOCKER_VERSION_TAG: '${{ steps.commit_args.outputs.version }}'
      #   run: bash docker/build --push --skip-build
