apiVersion: v1
kind: Service
metadata:
  name: {{ include "zairflow-helm.fullname" . }}
  labels:
    {{- include "zairflow-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zairflow-helm.selectorLabels" . | nindent 4 }}
