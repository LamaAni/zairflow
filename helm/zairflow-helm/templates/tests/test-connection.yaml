apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zairflow-helm.fullname" . }}-test-connection"
  labels:
    {{- include "zairflow-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zairflow-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
